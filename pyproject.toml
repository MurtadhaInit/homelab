[project]
name = "homelab"
version = "0.1.0"
description = "Python dependencies for Ansible and its community roles"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "ansible>=13.3.0",
  "ansible-lint>=26.1.1",
  "proxmoxer>=2.2.0",
  "requests>=2.32.5",
]
